# Keep real values only in .env, which is ignored by Git.
CHAT_B_AI_API_KEY=your-ai-api-key
# Optional for OpenAI-compatible providers. Leave unset if the provider supplies a default model.
CHAT_B_AI_API_URL=
CHAT_B_AI_MODEL=
